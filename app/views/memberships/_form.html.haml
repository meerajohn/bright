= simple_form_for @membership, defaults: {input_html: { class: "form-control" }} do |f|
  .row
    .col-md-12
      %p#notice= notice
      .text-right
        = f.button :submit, class: 'btn-primary'
        = link_to @membership do
          %button.btn.btn-default{type: "button"} Cancel

  = f.error_notification

  .form-inputs
    .row
      .col-md-6
        = f.input :code
      .col-md-6
        = f.input :name
    .row
      .col-md-6
        = f.input :kind,                                |
          label: "Type",                                |
          collection: Settings.membership.kinds,        |
          input_html: {class: "form-control selectize"} |
    .row
      .col-md-12
        = f.input :notes

  .form-actions
    = f.button :submit, class: 'btn-primary'
    = link_to @membership do
      %button.btn.btn-default{type: "button"} Cancel
