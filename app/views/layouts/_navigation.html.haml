%ul.nav.nav-sidebar
  %li{class: is_active?([:home])}
    %a{href: root_path} Overview
  %li{class: is_active?([:reports])}
    %a{href: reports_path} Reports
  %li{class: is_active?([:issues])}
    %a{href: issues_path} Issue
  %li{class: is_active?([:reservations])}
    %a{href: reservations_path} Reservation
%ul.nav.nav-sidebar
  %li{class: is_active?([:creators])}
    %a{href: creators_path} Creators
  %li{class: is_active?([:categories])}
    %a{href: categories_path} Categories
  %li{class: is_active?([:items])}
    %a{href: items_path} Items
  %li{class: is_active?([:copies])}
    %a{href: copies_path} Copies
%ul.nav.nav-sidebar
  %li{class: is_active?([:memberships])}
    %a{href: memberships_path} Memberships
  %li{class: is_active?([:members])}
    %a{href: members_path} Members
%ul.nav.nav-sidebar
  %li{class: is_active?([:users])}
    %a{href: users_path} Users
  %li{class: is_active?([:settings])}
    %a{href: ""} Settings
